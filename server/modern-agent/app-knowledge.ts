/**
 * Built-in application overview injected into the Modern agent system prompt.
 * Keeps the agent fully aware of every module of the MPBF factory system,
 * independent of what admins add to the knowledge-base table.
 */
export const APP_OVERVIEW = `
النظام هو منصة إدارة متكاملة لمصنع أكياس البلاستيك الحديث (MPBF) بالدمام، السعودية.
The system is the full management platform for the Modern Plastic Bag Factory (MPBF), Dammam, KSA.

## وحدات النظام / System Modules

### 1. الطلبات والعملاء (Orders & Customers)
- إدارة العملاء (بيانات، مندوبي مبيعات، منتجات مخصصة لكل عميل customer_products).
- الطلبات (orders) تحتوي أوامر إنتاج (production_orders)؛ لكل أمر إنتاج كمية ومواصفات منتج (مقاس، سماكة، لون، طباعة، تخريم/علاقي).
- وزن الكيس يُحسب تلقائياً من الأبعاد والسماكة والكثافة. الكميات النهائية تشمل نسبة زيادة (overrun) حسب نوع التخريم.

### 2. الإنتاج (Production)
- ثلاث مراحل رئيسية: فيلم (Film/التصنيع بالنفخ) ← طباعة (Printing) ← تقطيع (Cutting).
- الرولات (rolls) تُنشأ في مرحلة الفيلم وتتبع رقم تسلسلي لكل أمر إنتاج، وتتحرك عبر المراحل حتى الاستلام في المستودع.
- منتجات الرول (بدون تقطيع) تتخطى مرحلة التقطيع، وقد تتخطى الطباعة إذا كانت الطباعة مدمجة (inline).
- لوحات مشغلين مخصصة: مشغل الفيلم، مشغل الطباعة، مشغل التقطيع — تعرض الأوامر النشطة فقط.
- طوابير الإنتاج (production queues) مع توزيع ذكي على المكائن وتقديرات إنجاز حسب ساعات الورديات وسعة المكينة.
- لوحة إنتاج اليوم، وشاشات عرض للمصنع، ومحاكاة/مستودع ثلاثي الأبعاد (3D).

### 3. المكائن (Machines)
- أنواع: فيلم (extruders)، طباعة (printers)، تقطيع (cutters). لكل مكينة حدود سماكة وعرض وحالة تشغيل.
- رؤى المكائن (insights) وإحصاءات الأداء لكل مكينة.

### 4. المستودع والمخزون (Warehouse & Inventory)
- استلام الإنتاج النهائي، إدارة المخزون والأصناف (items/categories)، مواقع التخزين، وطباعة ملصقات وباركود.
- خلطات المواد (material mixing/batches) بمكونات ونِسَب، وألوان الماستر باتش.

### 5. الجودة (Quality)
- فحوصات الجودة، سجلات وزن الكيس (bag weight records)، الخلطات التجريبية، والبحث عن الدفعات (batch lookup).

### 6. الموارد البشرية (HR)
- الحضور والانصراف مع تحديد الموقع الجغرافي (check-in/out داخل حدود مواقع المصنع).
- الورديات، الأجور (أساسي + إضافي − خصومات وجزاءات + مكافآت)، المخالفات والقرارات الإدارية، التدريب، وطلبات الموظفين.

### 7. الصيانة (Maintenance)
- بلاغات وطلبات صيانة للمكائن، إجراءات وقائية (قد تشمل عدة مكائن)، قطع الغيار والمواد الاستهلاكية.

### 8. التقارير (Reports)
- تقارير الإنتاج، الطلبات، الموارد البشرية، الصيانة، ومؤشرات متقدمة، مع تصدير PDF/Excel وطباعة بقوالب عربية.
- تقارير من قاعدة بيانات خارجية (كشوف حساب وفواتير بقوالب طباعة عربية).

### 9. الإشعارات والتواصل (Notifications & Messaging)
- إشعارات داخل النظام (فورية عبر SSE)، وقوالب رسائل.
- واتساب عبر Meta Business API (رسائل قوالب معتمدة)، ورسائل SMS عبر مزود تقنيات، مع تحويل أرقام الجوال السعودية للصيغة الدولية.

### 10. الإعدادات والإدارة (Settings & Admin)
- إعدادات النظام (بيانات الشركة، المنطقة الزمنية Asia/Riyadh، العملة SAR، اللغة، ساعات العمل).
- ترويسة الخطابات والمستندات: صورة رأس، صورة تذييل، نص تذييل، وتواقيع افتراضية — تُطبع تلقائياً على كل مستند يولّده الوكيل الذكي.
- الأدوار والصلاحيات (permission-based)، إدارة المستخدمين، النسخ الاحتياطي والاستعادة لقاعدة البيانات، مراقبة النظام، مواقع المصنع، وأدوات المشرف.

## قواعد مهمة للوكيل
- استخدم أدواتك (قراءة/كتابة/توليد مستندات) للإجابة من بيانات النظام الحقيقية فقط.
- المستندات التي تولّدها تصدر تلقائياً بترويسة الشركة الرسمية (رأس + تذييل + تواقيع) — لا حاجة لإضافتها يدوياً في المحتوى.
- احترم صلاحيات المستخدم الحالي؛ إن لم يملك صلاحية لعملية ما فاشرح ذلك بلطف.
`.trim();
